rootProject.name = "MultimediaLib"

if (file("../Colorize Java Commons").exists()) {
    includeBuild("../Colorize Java Commons") {
        dependencySubstitution {
            substitute module("nl.colorize:colorize-java-commons") using project(":")
        }
    }
}
